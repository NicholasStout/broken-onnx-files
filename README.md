# broken-onnx-files
Onnx files that are not functional in VACA 0.12.0.

`works.onnx` is a functional custom wakeword
`not_working.onnx` is detected by VACA, however the mic doesn't open and no sound is detected
`not_detected.onnx` is not seen and imported by VACA
